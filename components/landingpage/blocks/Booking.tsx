/**
 * Booking — placeholder.
 *
 * Phase 1 ships the model + dispatcher only. The real renderer lands in
 * Phase 6 (Services layout — priority layout) where this block gets its
 * first visual home (Calendly / Cal.com embed or fallback form).
 *
 * Expected ``data`` shape: ``{ embed_url: string, heading?: string, subheading?: string }``.
 */

export default function Booking(_props: { data: Record<string, unknown> }) {
  return null;
}
