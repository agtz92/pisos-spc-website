/**
 * TrustStrip — placeholder.
 *
 * Phase 1 ships the model + dispatcher only. The real renderer lands in
 * Phase 4 (all layouts — short strip just below the hero with rating,
 * customer count, and other trust signals).
 *
 * Expected ``data`` shape: ``{ text: string }``.
 */

export default function TrustStrip(_props: { data: Record<string, unknown> }) {
  return null;
}
